"""App package for TierOptimizer backend.

Keep this file minimal to allow Python to import the `app` package during CI and tests.
"""

# Intentionally minimal; submodules are imported directly where needed.

